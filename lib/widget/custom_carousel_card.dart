import 'package:flutter/material.dart';

class CustomCarouselCard extends StatelessWidget {
  const CustomCarouselCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 150,
      color: Colors.grey,
    );
  }
}
